---
import { getPostUrl } from '@root/util/query';
import type { CollectionEntry } from 'astro:content';
import FormattedDate from '../dates/FormattedDate.astro';

interface Props {
  isActivePost: boolean,
  showHeadings?: boolean,
  post: CollectionEntry<'blog'>
}

const { 
  post,
  isActivePost,
  showHeadings = false
} = Astro.props;

const { headings } = await post.render();

const appendixLabels = "ABCDEFGHIJKLMNOPQRSTUV".split("")

const series = post.data.series!;
const label = series.isAppendix ?
  `Appendix ${appendixLabels[series.seriesNumber-1]}` :
  `Part ${series.seriesNumber}`;
---

<div class={`post ${isActivePost ? "active" : ""}`}>
  <div class="post-date">
    <div class="post-date-label">
      <FormattedDate date={post.data.datePublished} />
    </div>
  </div>
  <div>
    <div class="post-label">
      {label}, <a class="post-title" href={getPostUrl(post)}>{post.data.title}</a>
    </div>
  {
    showHeadings && headings && <div>
      {headings.map(h =>
        <div class="post-heading" style={`--heading-depth: ${h.depth}`}>
          <a href={`#${h.slug}`}>{h.text}</a>
        </div>
      )}
    </div>
  }
  </div>
</div>

<style is:inline>
.post {
  display: grid;
  grid-template-columns: subgrid;
  grid-column: span 2;
}

.post-title {
	display: inline-block;
	font-size: var(--font-size-normal);
	margin-bottom: 0.2em;
}

.post.active .post-label, .post.active .post-date-label {
	font-weight: var(--font-bold-weight);
	background-color: #ccfd8e;
}

.post.active .post-title {
	font-weight: var(--font-bold-weight);
}

.post-date {
	color: gray;
	display: inline-block;
	width: 8.0em;
}

.post-date-label {
	padding-left: 1.0em;
}

.post-heading {
  padding-left: calc(var(--heading-depth)*1.4em);

  color: var(--font-normal-color);
}

.post-heading:hover {
  background-color: rgb(208, 231, 184);
}

.post-heading:hover a {
  text-decoration: underline;
}

.post-heading a {
  color: inherit;
}
</style>