---
title: Minimal Example of Rust-Wasm with WebGL and Specs
titleDisplay: Minimal Example of Rust&#8209;Wasm with WebGL and <code>specs</code>
datePublished: 2024/08/01
summary: |
  A minimal demo page for `rust-wasm`.
  ```cpp
  def random_orthogonal(k):
    A = np.random.randn(k,k)
    Q,_ = np.linalg.qr(A)
    return Q;
  ```
tags: [code, graphics]
tools: [WebGL, Rust]
published: true
---

import { Demo } from "./demo/Demo.tsx";

# Rust-Wasm Example!

The following demo is implemented in Rust and compiled to WASM using `wasm-bindgen`.  WebGL rendering is achieved with `glow`.  Uses the `specs` entity-component-system library for managing game state.  Longer writeup coming soon!

<Demo client:only="solid" />
